# Rename this file to ".env"
# No API key needed — Jarvis runs on a free local AI model via Ollama.

# Name you want to call your assistant (used in logs)
ASSISTANT_NAME=Jarvis

# Which local Ollama model to use (must support tool calling)
OLLAMA_MODEL=llama3.1
